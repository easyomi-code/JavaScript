// 논리 연산자(AND, OR, NOT)
// JavaScript는 다른 언어와 다르게 피연산자가 불리언(boolean) 타입이 아닐때도 논리 연산이 가능하다.
// 값이 있는 것을 true로 간주하기 때문

// AND(&&) 연산 - 피연산자가 모두 true일 경우에 ture 반환, 나머지 false
let x = true;
let y = true;
console.log(x && y);
